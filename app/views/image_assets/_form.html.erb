<%= form_with(model: image_asset, local: true) do |form| %>
  <% if image_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_asset.errors.count, "error") %> prohibited this image_asset from being saved:</h2>

      <ul>
        <% image_asset.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :approval %>
    <%= form.text_field :approval %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
